{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
      "deploy": "echo 'anxcye.com' > dist/CNAME && push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "pinia": "^2.1.7",
    "vue": "^3.3.11",
    "vue-router": "^4.2.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.5.2",
    "push-dir": "^0.4.1",
    "vite": "^5.0.10"
  }
}
